'use strict';

var BASE_JS_PATH = '../../src';

var chai = require( 'chai' );
var expect = chai.expect;
var domClosest = require( BASE_JS_PATH + '/utilities/dom-closest' );
var jsdom = require( 'mocha-jsdom' );

describe( 'dom-closest', function() {
  jsdom();
  // TODO: Implement tests.
} );
